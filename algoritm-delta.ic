void main() {
	
	long sleepTimeMs = 250L;
    
    int portCenter = 4;
    int rangeCenter = 0;

    while(1) {
        msleep(sleepTimeMs);
        
        rangeCenter = sensorRange(portCenter);
        
        printf("R: %d | L: %d     C: %d\n", rangeRight, rangeLeft, rangeCenter);
    }
}